# Start Development Environment

1. Check if `node_modules` exists. If not, run `pnpm install`.
2. Run `pnpm dev` to start all dev servers via Turborepo.
3. Report which services started and their local URLs.
4. If port conflicts or errors occur, diagnose and report.
